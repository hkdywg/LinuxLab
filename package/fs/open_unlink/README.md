**NOTE!**

# 应用软件中，文件在open后进行read/write操作，如果在没有close文件之前进行delete操作，这样操作系统会删除file对应的dentry信息，但inode没有删除，已经打开的文件依旧可以
# 访问，所以在文件系统中使用类似 ``ls`` 命令的话，看起来文件是已经被删除了，但是文件系统的容量并没有减少。所以如果后续创建文件时，可能会存在容量不足的问题。











